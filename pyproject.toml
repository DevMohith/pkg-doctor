[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "dep-doctor"
version = "0.1.0"
description = "Scan local projects for known-vulnerable or malicious dependencies, and get guided help rotating exposed API keys."
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.10"
authors = [
  { name = "Mohith Tummala" }
]
keywords = ["security", "dependencies", "vulnerability", "supply-chain", "secrets", "cli", "osv", "sbom"]
classifiers = [
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Operating System :: OS Independent",
  "Topic :: Security",
  "Environment :: Console",
]
dependencies = [
  "click>=8.0",
  "requests",
  "pyyaml",
  "tomli>=2.0; python_version < '3.11'",
]

[project.scripts]
dep-doctor = "dep_doctor.cli:cli"

[project.urls]
Homepage = "https://github.com/DevMohith/dep-doctor"
Issues   = "https://github.com/DevMohith/dep-doctor/issues"

[tool.setuptools.packages.find]
include = ["dep_doctor*"]
